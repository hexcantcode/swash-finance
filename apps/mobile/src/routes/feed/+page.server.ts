/** /feed loaded client-side from /api/feed/latest-trades and
 *  /api/feed/top-open-positions. Stub stays so SvelteKit treats the route
 *  as having no server load. */
export const prerender = false;
