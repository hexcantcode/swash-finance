/** /trader/[address] loaded client-side from /api/leaders/[address]. Stub
 *  stays so SvelteKit treats the route as having no server load. The
 *  [address] param is consumed in +page.svelte via $page.params. */
export const prerender = false;
