/** Profile is a client-only placeholder for now (wallet connect + mirrored
 *  positions land here later). Stub keeps the route server-load-free, matching
 *  the other screens. */
export const prerender = false;
